# GTPB 版本更新与常见问题（VERSION FAQ）

> 本文档记录 GameToyProtocolBridge (GTPB) 的版本更新内容、新功能使用指南与常见问题解答。
> 其他语言版本：英文见 [README.md](README.md)，另有 9 种语言 README 可供查阅。

---

## 当前版本

**V1.0.1**

---

## 版本历史

### V1.0.1（本次迭代）

针对 TryFun Meta2 等「速度型活塞」设备无法在 Intiface Central 中自定义行程距离的问题，新增了一套模拟 OSR6 行程式运动的能力，并同步优化了交互体验。

**新增功能：**

1. **行程模拟（Pulse / 行程模拟）**
   - 原理：速度型活塞设备在收到非 0 信号后持续运动、收到 0 才停止；而 Intiface Central 中 oscillate 滑块归 0 时设备会复位到初始位置。
   - 本软件利用这一点：收到非 0 信号 → 推进 → 延迟发送归 0 → 设备复位，从而把「速度」模拟成「行程」，近似 OSR 系列的往复运动。
   - 每个通道可独立开启，复位延时（`pulse_ms`）可调。

2. **通道延时（Channel Delay / 通道延时）**
   - 参考 MultiFunPlayer：对每个通道单独设置发送延时，可正可负。
   - 正数 = 滞后发送；负数 = 提前发送（以所有通道中最负的延时为基线，保证相对时序准确）。
   - 用于校准多轴设备的信号时序。

3. **仅变化转发（Dedupe / 仅变化转发）**
   - 游戏若持续发送同一数值，会导致行程模拟反复触发「推进 → 复位」。
   - 勾选后：相同数值不再转发，只有数值发生变化才发送。配合行程模拟可让设备在「推进一次 → 复位」后停住，直到游戏发送新数值。

4. **信号强度柱状图（Signal Meter）**
   - 映射页底部实时柱状图，按执行器类型着色（Vibrate 蓝 / Rotate 橙 / Linear 绿），柱顶显示实时数值。
   - 刷新率约 33fps，直观观察游戏输入信号。

5. **悬停提示（Tooltip）**
   - 鼠标悬停在每一项设置（连接、映射、按钮、柱状图等）上时，延迟弹出该选项的作用说明。

6. **版本号显示**
   - 窗口标题显示 `GameToyProtocolBridge (GTPB)  V1.0.1`。

---

## 新功能使用指南

### 1. 如何让 Meta2 这类速度型活塞实现「行程式」运动？

1. 在「映射」页勾选对应通道的 **行程模拟**。
2. 设置 **复位延时**（ms）：数值越小往复越快，越大往复越慢；建议从 200ms 开始调试。
3. 若游戏会持续发送同一数值，同时勾选 **仅变化转发**，避免设备反复自动触发。

### 2. 「仅变化转发」什么时候用？

当你在游戏里把某个冲程参数固定为同一数值，却发现设备在反复自动活塞时，说明游戏可能在持续重复发送同一数值。勾选该通道的 **仅变化转发** 后，只有数值变化才转发，可消除反复触发。

### 3. 通道延时正负怎么理解？

- **正延时**：该通道的信号比其他通道「晚」发送（滞后）。
- **负延时**：该通道的信号比其他通道「早」发送（提前）。
- 负延时的实现以所有通道中最负的延时为基准，其余通道相应推迟，从而保证相对顺序正确。

---

## 常见问题（FAQ）

### Q1：两个端口（12345 / 12346）如何配置？

- **WebSocket 端口（默认 12345）**：游戏端连接本软件的端口，一般保持不变。
- **后端 Intiface（默认 ws://127.0.0.1:12346）**：本软件连接 Intiface Central 的地址。
- 两个端口必须不同，否则数据流会形成回路。可在 Intiface Central → 设置 → 服务器 → 监听端口 中调整（建议 12346），然后同步修改本软件「后端 Intiface」。

### Q2：开启行程模拟后没有反应，或往复过快/过慢？

- 确认对应通道已勾选「行程模拟」并已「保存 Profile」。
- 往复速度由「复位延时」控制：过快就加大数值，过慢就减小数值。
- 若设备毫无反应，先检查是否已连接 Intiface Central、设备是否已发现，以及通道是否映射到了正确执行器。

### Q3：断开连接后设备自己转起来（空转）？

这是 Intiface Central / Buttplug 在连接断开时的已知兼容性问题（会向设备补发残留非零状态）。本软件已尽力缓解，但 B 端（Intiface Central → 设备）侧的行为无法从本软件彻底控制，请关注 Intiface Central 后续版本修复。

### Q4：切换语言后界面没有更新？

切换语言会重建整个界面，映射设置保留。若发现翻译缺失，检查 `gtpb/i18n.py` 中对应语言的翻译键是否齐全。

### Q5：如何打包成 EXE？

```bash
pip install pyinstaller
pyinstaller gtpb.spec
```

---

## 目录结构

```
gtpb-python/
├── gtpb/                    # 核心模块
│   ├── backend.py           # Intiface 后端连接
│   ├── buttplug.py          # Buttplug v3 协议解析
│   ├── config.py            # 配置加载（INI + JSON Profile）
│   ├── gui.py               # Tkinter 图形界面
│   ├── i18n.py              # 多语言支持
│   ├── logs.py              # 日志管理
│   ├── mapping.py           # 通道映射引擎
│   ├── models.py            # 数据模型
│   ├── proxy.py             # 桥接服务核心
│   ├── pulse.py             # 行程模拟 + 通道延时 + 电平采样（V1.0.1 新增）
│   ├── safety.py            # 安全机制（急停）
│   └── transform.py         # 数值变换
├── profiles/default.json    # 默认 Profile
├── tests/                   # 单元测试
├── tools/                   # 开发辅助工具
├── configsetting.ini        # 出厂配置（只读，勿改）
├── main.py                  # 程序入口
└── requirements.txt         # Python 依赖
```
